# Test Environment Variables
# These are dummy values for fast local testing without external dependencies

# Test mode flag - ALWAYS true for mocked auth
NEXT_PUBLIC_TEST_MODE=true

# Clerk URLs (required but not used in test mode)
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Dummy Clerk Environment Variables (not used in test mode)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_dummy_key_for_testing
CLERK_SECRET_KEY=sk_test_dummy_secret_for_testing
CLERK_WEBHOOK_SECRET=whsec_test_dummy_webhook_secret

# Dummy Supabase Environment Variables (not used in test mode)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy_anon_key_for_testing
SUPABASE_SERVICE_ROLE_KEY=dummy_service_role_key_for_testing

# Dummy Database URL (not used in test mode)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test

# No ngrok needed for local testing
NGROK_AUTHTOKEN=

